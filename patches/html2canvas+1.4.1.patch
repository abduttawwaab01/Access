diff --git a/node_modules/html2canvas/dist/html2canvas.esm.js b/node_modules/html2canvas/dist/html2canvas.esm.js
index 268e9bc..175d18f 100644
--- a/node_modules/html2canvas/dist/html2canvas.esm.js
+++ b/node_modules/html2canvas/dist/html2canvas.esm.js
@@ -1828,11 +1828,21 @@ var hsl = function (context, args) {
     var b = hue2rgb(t1, t2, h - 1 / 3);
     return pack(r * 255, g * 255, b * 255, a);
 };
+var unsupportedFallback = function (_context, _args) {
+    return pack(128, 128, 128, 1);
+};
 var SUPPORTED_COLOR_FUNCTIONS = {
     hsl: hsl,
     hsla: hsl,
     rgb: rgb,
-    rgba: rgb
+    rgba: rgb,
+    oklab: unsupportedFallback,
+    oklch: unsupportedFallback,
+    lab: unsupportedFallback,
+    lch: unsupportedFallback,
+    color: unsupportedFallback,
+    hwb: unsupportedFallback,
+    'color-mix': unsupportedFallback
 };
 var parseColor = function (context, value) {
     return color$1.parse(context, Parser.create(value).parseComponentValue());
diff --git a/node_modules/html2canvas/dist/html2canvas.js b/node_modules/html2canvas/dist/html2canvas.js
index 3ec00e0..7cbccf5 100644
--- a/node_modules/html2canvas/dist/html2canvas.js
+++ b/node_modules/html2canvas/dist/html2canvas.js
@@ -1834,11 +1834,21 @@
         var b = hue2rgb(t1, t2, h - 1 / 3);
         return pack(r * 255, g * 255, b * 255, a);
     };
+    var unsupportedFallback = function (_context, _args) {
+        return pack(128, 128, 128, 1);
+    };
     var SUPPORTED_COLOR_FUNCTIONS = {
         hsl: hsl,
         hsla: hsl,
         rgb: rgb,
-        rgba: rgb
+        rgba: rgb,
+        oklab: unsupportedFallback,
+        oklch: unsupportedFallback,
+        lab: unsupportedFallback,
+        lch: unsupportedFallback,
+        color: unsupportedFallback,
+        hwb: unsupportedFallback,
+        'color-mix': unsupportedFallback
     };
     var parseColor = function (context, value) {
         return color$1.parse(context, Parser.create(value).parseComponentValue());
diff --git a/node_modules/html2canvas/dist/lib/css/types/color.js b/node_modules/html2canvas/dist/lib/css/types/color.js
index 50608c3..ac12c64 100644
--- a/node_modules/html2canvas/dist/lib/css/types/color.js
+++ b/node_modules/html2canvas/dist/lib/css/types/color.js
@@ -124,11 +124,21 @@ var hsl = function (context, args) {
     var b = hue2rgb(t1, t2, h - 1 / 3);
     return exports.pack(r * 255, g * 255, b * 255, a);
 };
+var unsupportedFallback = function (_context, _args) {
+    return exports.pack(128, 128, 128, 1);
+};
 var SUPPORTED_COLOR_FUNCTIONS = {
     hsl: hsl,
     hsla: hsl,
     rgb: rgb,
-    rgba: rgb
+    rgba: rgb,
+    oklab: unsupportedFallback,
+    oklch: unsupportedFallback,
+    lab: unsupportedFallback,
+    lch: unsupportedFallback,
+    color: unsupportedFallback,
+    hwb: unsupportedFallback,
+    'color-mix': unsupportedFallback
 };
 var parseColor = function (context, value) {
     return exports.color.parse(context, parser_1.Parser.create(value).parseComponentValue());
